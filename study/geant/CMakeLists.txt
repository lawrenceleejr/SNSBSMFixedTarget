cmake_minimum_required(VERSION 3.16)
project(flash)
find_package(Geant4 REQUIRED)
include(${Geant4_USE_FILE})
add_executable(flash flash.cc)
target_link_libraries(flash ${Geant4_LIBRARIES})
add_executable(flash_damsa flash_damsa.cc)
target_link_libraries(flash_damsa ${Geant4_LIBRARIES})
